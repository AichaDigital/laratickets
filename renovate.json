{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":timezone(Europe/Madrid)",
    ":semanticCommits"
  ],
  "schedule": ["before 10am on monday"],
  "packageRules": [
    {
      "description": "Pin the MySQL CI service container to the 8.4 LTS line. MySQL 9.x is an Innovation release (short support window); keep the LTS track. Reverted twice before (#17, #25).",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["mysql"],
      "allowedVersions": "<9"
    }
  ]
}
